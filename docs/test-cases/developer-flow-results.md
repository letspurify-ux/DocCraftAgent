# 신규 개발자 흐름 문서 테스트 결과

2026-09-13에 이번 검증을 종료했다. 추가 수정·재생성은 진행하지 않는다.

## 판정

프로그램은 실제 소스로 문서 생성과 반복 검토를 끝냈지만, 자동 결과는 신규 개발자에게 그대로 제공할 품질 기준을 충족하지 못했다. 인용과 다이어그램 문법 검증은 의미 정확성을 보장하지 않는다.

- 자동 결과: `/Users/iceblue/workspace/llm_agent_developer_flow.md`
- 소스를 직접 대조해 별도로 작성한 검토본: `/Users/iceblue/workspace/llm_agent_developer_flow_reviewed.md`
- 검토본은 자동 생성 성능 평가 결과에 포함하지 않는다.

## 실제 실행

| 항목 | 결과 |
| --- | --- |
| 실행 ID | f3f975f8-5981-42fd-a4f9-8bb465865cb7 |
| 소스 | /Users/iceblue/workspace/llm_agent |
| 모델 | OpenRouter / deepseek/deepseek-v4-flash-0731 |
| 설정 | 추론 off, 출력 16,384, 컨텍스트 200,000, 작업 총 한도 10,000,000 |
| 시작 / 종료 | 2026-09-13 09:53:39 / 10:35:11 KST |
| 경과 | 약 41분 32초 — 디버깅·재시작 포함, 순수 생성 속도 측정값 아님 |
| 상태 | completed_with_warnings |
| 누적 토큰 집계 | 1,317,383 — 실패 요청의 보수적 예약 포함, 실제 과금량과 다를 수 있음 |
| 산출물 | 4개 장, Mermaid 2개, 42,939바이트 |
| 근거 | 12개 파일, 37개 청크; 실행 스냅샷 대비 소스 변경 없음 |
| 요청 컨텍스트 최대 예약 | 입력·출력·안전 여유 합계 188,643 / 200,000 |
| 보고된 최대 출력 | 4,026토큰 |

최종 자동 검토에는 major 12개, minor 5개가 남았다. 검토 모델 자체의 오판도 포함되어 있으므로 이 숫자는 독립적으로 검증된 오류 개수가 아니다. 파일 제외/읽기 실패 보고는 2,829개다. 목적 문구도 실제 진입 파일과 함수 이름을 포함하도록 보완했으므로 이전 결과와의 비교는 통제된 성능 실험이 아니다.

## 자동 결과의 주요 잔여 문제

1. 전체 시퀀스에서 실제 `run_query/run_queries` 분기를 누락했다.
2. `history`를 유일한 상태라고 설명하고, `runQuery` 함수 이름 및 예시 식별자의 일관성을 잘못 표현했다.
3. 검색 이후 조건과 쿼리 실행을 임의로 연결한 다이어그램이 남았으며, 저장 결과를 읽는 분기도 충분히 표현하지 못했다.
4. 서버 오류 처리 코드를 요청 진입점으로 인용하고, 주기적 임베딩 동기화를 요청 중 검색 경로에 섞었다.

반복 검토가 일부 오류를 고치면서 다른 설명을 악화시키는 현상도 확인했다. 무인 실행 완료는 확인했지만, 무인 생성 문서의 사실 정확성까지 충족했다고 판단할 수 없다.

## 반영한 개선과 검증

파일 목록·실제 함수 이름 제공, 지정 파일 후속 근거 검색, 검색어 중심 발췌, 구현 우선 작성 지침, 다이어그램 상한 설정, 전역 검토 JSON 복구, 반복 검토 체크포인트 복구, 짧은 근거 ID 전달, Mermaid 구문 오류 상세 피드백을 반영했다.

Rust 테스트 30개, Clippy, 프론트엔드 타입 검사·빌드, Playwright 5개 및 로컬 MariaDB 통합 검사를 통과했다. 통합 검사에는 컨텍스트·출력·총 토큰 한도, 취소(약 0.243초), 부분 결과 재개, 검토 중 재시작, 외부 파일 충돌 및 DB 장애 복구가 포함됐다. 이는 검사한 시나리오의 결과이며 모든 오류나 교착 부재를 증명하지 않는다. 마지막 Mermaid 오류 상세 변경은 빌드·검증을 마쳤지만 실행 중 서버에는 재기동으로 반영하지 않았다.

완성된 자동 산출물의 근거 해시·행 범위·인용 및 Mermaid 구문 검사에서 문제가 발견되지 않았다. 별도 검토본은 소스 참조 11개 범위를 대조했고, 실제 프론트엔드 렌더러에서 다이어그램 2개와 각주 참조 45개를 확인했다. 화면 오류는 없었다.

검증 기록: `.local/developer-verification.json`, `.local/developer-reviewed-source-check.json`, `.local/developer-render-check.json`, `docs/smoke-results.json`.
